export const ROW_HEIGHT = 49;
/** The first work-center row is taller than the rest: 49 + 11 = 60px. */
export const FIRST_ROW_EXTRA = 11;
export const BAR_HEIGHT = 38;
/** Width of the sticky "Work Center" column; must match $centers-width in timeline.component.scss. */
export const CENTERS_WIDTH = 381;
export const BAR_TOP = (ROW_HEIGHT - BAR_HEIGHT) / 2;
/** Height of the sticky header band; must match $header-height in timeline.component.scss. */
export const HEADER_HEIGHT = 34;
export const TIP_HEIGHT = 26;
export const TIP_GAP = 5;
